# NOTE: cairosvg also needs the native cairo library, which pip cannot install:
#   macOS:  brew install cairo
#   Ubuntu: sudo apt install libcairo2
cairosvg>=2.7
pypdf>=4.0
reportlab>=4.0
pillow>=10.0
pyyaml>=6.0
